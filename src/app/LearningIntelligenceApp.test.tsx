import{render,screen}from'@testing-library/react';import userEvent from'@testing-library/user-event';import{beforeEach,describe,expect,it}from'vitest';import App from'./LearningIntelligenceApp';
const at=(p='/')=>{history.replaceState({},'',p);return render(<App/>)};describe('P-001A cinematic role-aware shell',()=>{beforeEach(()=>history.replaceState({},'','/'));it('renders ordered navigation and cinematic teacher home',()=>{at();expect(screen.getByRole('heading',{name:/Help every learner/i})).toBeInTheDocument();const links=screen.getByRole('navigation',{name:'Primary navigation'}).querySelectorAll('a');expect(Array.from(links).map(x=>x.textContent?.replace(/\d|↗/g,'').trim())).toEqual(['Home','Learners','Teachers','Interventions','Outcomes','Knowledge'])});it('changes the composition for every role',async()=>{const u=userEvent.setup();at();const role=screen.getByRole('combobox',{name:'Demo role'});await u.selectOptions(role,'learner');expect(screen.getByRole('heading',{name:/Build momentum/i})).toBeInTheDocument();await u.selectOptions(role,'operations');expect(screen.getByRole('heading',{name:/Keep human support moving/i})).toBeInTheDocument();await u.selectOptions(role,'governance_admin');expect(screen.getByRole('heading',{name:/Make every intelligent action reviewable/i})).toBeInTheDocument()});it('preserves timeline provenance',()=>{at('/learners/maya');expect(screen.getByText(/Learning event · Observed/)).toBeInTheDocument();expect(screen.getByText(/Teacher observation · Teacher-reported/)).toBeInTheDocument();expect(screen.getByText(/Reduces concern/)).toBeInTheDocument()});it('shows honest cinematic preview',()=>{at('/knowledge');expect(screen.getByText(/Not implemented yet/)).toBeInTheDocument();expect(screen.getByText(/EXPECTED EVIDENCE/)).toBeInTheDocument()});it('keeps executive route private',async()=>{const u=userEvent.setup();at('/strategic/executive');expect(screen.getByRole('heading',{name:/do not have access/i})).toBeInTheDocument();await u.selectOptions(screen.getByRole('combobox',{name:'Demo role'}),'executive');await u.click(screen.getByRole('button',{name:/Enable executive/i}));history.pushState({},'','/strategic/executive');dispatchEvent(new PopStateEvent('popstate'));expect(await screen.findByRole('heading',{name:'Executive Experience',level:1})).toBeInTheDocument()});it('switches shell language',async()=>{const u=userEvent.setup();at();await u.selectOptions(screen.getByRole('combobox',{name:'Language'}),'es');expect(screen.getAllByRole('link',{name:/Inicio/}).length).toBeGreaterThan(0);expect(screen.getAllByText(/no es autenticación/i).length).toBeGreaterThan(0)})});
